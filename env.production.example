# ============================================================================
# Production Environment Variables Example
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!
# ============================================================================

# ============================================================================
# Database Configuration
# ============================================================================
DATABASE_URL="postgresql://user:password@host:port/database"

# ============================================================================
# NextAuth Configuration
# ============================================================================
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-secret-key-here"

# ============================================================================
# API Configuration - V1 (football-data.org) - DEFAULT
# ============================================================================
# This is the default API provider
FOOTBALL_DATA_API_KEY="your-football-data-api-key"

# ============================================================================
# API Configuration - V2 (api-sports.io) - NEW
# ============================================================================
# To use V2 API, set USE_API_V2=true and provide API-FOOTBALL key
# Note: Variable name uses HYPHEN, not underscore
API-FOOTBALL="your-api-sports-football-key"

# Optional: Separate key for Rugby API (falls back to API-FOOTBALL if not set)
API-RUGBY="your-api-sports-rugby-key"

# Feature flag: Set to true to use V2 API, false or omit to use V1 (default)
USE_API_V2=false

# ============================================================================
# Other Configuration
# ============================================================================
NODE_ENV="production"

# ============================================================================
# Notes
# ============================================================================
# 1. If USE_API_V2 is not set or false, the system uses V1 (football-data.org)
# 2. If USE_API_V2=true, you MUST set API-FOOTBALL
# 3. Rugby API always uses api-sports.io (independent of USE_API_V2 flag)
# 4. API-RUGBY is optional - if not set, it falls back to API-FOOTBALL
# 5. Variable names with hyphens (API-FOOTBALL, API-RUGBY) must be quoted in shell
